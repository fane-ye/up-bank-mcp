[project]
name = "up-bank-mcp"
version = "1.0.0"
description = "Up Bank MCP server"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "mcp[cli]>=1.27.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-httpx>=0.35",
]
